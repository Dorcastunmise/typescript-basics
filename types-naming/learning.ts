//array types
let guests : string[] = ["flvc", "dhl", "sece"];

//boolean types
let africana : boolean;
africana = true

//function
// let seminar: () => {};
//--------or----------
// let conference: (title: string) => any;
const seminar = function(name: string) : string {
    return name.toUpperCase();
}
// -----or------
const conference = function(name: string) : void {
    // return name.toUpperCase();
    console.log(conference);
    
}